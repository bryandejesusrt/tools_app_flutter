import 'package:flutter/material.dart';

class AppBarTolls extends StatelessWidget {
  const AppBarTolls({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
